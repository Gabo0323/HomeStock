export interface Product {
  id: string;
  name: string;
  category: string;
  quantity: number;
  acquisitionDate: string; // o Date si prefieres manejarlo como objeto Date
  price: number;
  brand: string;
  purchaseLocation: string;
  imageUrl: string;
}